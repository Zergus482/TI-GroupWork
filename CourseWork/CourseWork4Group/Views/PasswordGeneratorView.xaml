<UserControl x:Class="CourseWork4Group.Views.PasswordGeneratorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Loaded="UserControl_Loaded">
    
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Заголовок -->
        <TextBlock Grid.Row="0" 
                   Text=" Генератор паролей" 
                   FontSize="32" 
                   FontWeight="Bold"
                   Foreground="#667eea"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,30"/>
        
        <!-- Ползунок для длины пароля -->
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Длина пароля:" 
                          FontSize="16" 
                          FontWeight="SemiBold"
                          Foreground="#555"
                          VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" 
                          x:Name="PasswordLengthLabel"
                          Text="16" 
                          FontSize="18" 
                          FontWeight="Bold"
                          Foreground="#667eea"
                          VerticalAlignment="Center"
                          Margin="15,0,0,0"
                          MinWidth="40"
                          TextAlignment="Center"/>
            </Grid>
            <Slider x:Name="PasswordLengthSlider"
                   Style="{StaticResource ModernSliderStyle}"
                   Minimum="8" 
                   Maximum="64" 
                   Value="16"
                   TickPlacement="BottomRight"
                   Margin="0,10,0,0"
                   ValueChanged="PasswordLengthSlider_ValueChanged">
                <Slider.Resources>
                    <Style TargetType="TickBar">
                        <Setter Property="Fill" Value="#667eea"/>
                        <Setter Property="Height" Value="8"/>
                    </Style>
                </Slider.Resources>
            </Slider>
            <Grid Margin="0,5,0,0">
                <TextBlock Text="8" FontSize="12" Foreground="#999" HorizontalAlignment="Left"/>
                <TextBlock Text="64" FontSize="12" Foreground="#999" HorizontalAlignment="Right"/>
            </Grid>
        </StackPanel>
        
        <!-- Чекбоксы для опций -->
        <GroupBox Grid.Row="2" 
                 Header="Параметры генерации" 
                 FontSize="16" 
                 FontWeight="SemiBold"
                 Foreground="#555"
                 Padding="15"
                 Margin="0,0,0,20"
                 BorderBrush="#E0E0E0"
                 BorderThickness="2">
            <StackPanel>
                <CheckBox x:Name="IncludeLowercaseCheckBox" 
                         Content="Использовать строчные буквы (a-z)" 
                         Style="{StaticResource ModernCheckBoxStyle}"
                         IsChecked="True"
                         Checked="CheckBox_Changed"
                         Unchecked="CheckBox_Changed"/>
                <CheckBox x:Name="IncludeUppercaseCheckBox" 
                         Content="Использовать заглавные буквы (A-Z)" 
                         Style="{StaticResource ModernCheckBoxStyle}"
                         IsChecked="True"
                         Checked="CheckBox_Changed"
                         Unchecked="CheckBox_Changed"/>
                <CheckBox x:Name="IncludeNumbersCheckBox" 
                         Content="Использовать цифры (0-9)" 
                         Style="{StaticResource ModernCheckBoxStyle}"
                         IsChecked="True"
                         Checked="CheckBox_Changed"
                         Unchecked="CheckBox_Changed"/>
                <CheckBox x:Name="IncludeSpecialCharsCheckBox" 
                         Content="Использовать специальные символы (!@#$%^&amp;*)" 
                         Style="{StaticResource ModernCheckBoxStyle}"
                         IsChecked="True"
                         Checked="CheckBox_Changed"
                         Unchecked="CheckBox_Changed"/>
            </StackPanel>
        </GroupBox>
        
        <!-- Кнопка генерации -->
        <StackPanel Grid.Row="3" Margin="0,0,0,20">
            <Button x:Name="GenerateButton"
                   Content="✨ Сгенерировать пароль" 
                   Style="{StaticResource ModernButtonStyle}"
                   Click="GenerateButton_Click"
                   Margin="0,0,0,10"/>
            
            <!-- Индикатор Pre и Post -->
            <Grid HorizontalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Ellipse x:Name="PreIndicator"
                        Grid.Column="0"
                        Width="12"
                        Height="12"
                        Margin="0,0,8,0"
                        VerticalAlignment="Center">
                    <Ellipse.Fill>
                        <SolidColorBrush x:Name="PreIndicatorBrush" Color="#F44336"/>
                    </Ellipse.Fill>
                </Ellipse>
                <TextBlock Grid.Column="1"
                          Text="Pre"
                          FontSize="20"
                          FontWeight="SemiBold"
                          Foreground="#555"
                          VerticalAlignment="Center"/>
                
                <Ellipse x:Name="PostIndicator"
                         Grid.Column="2"
                         Width="12"
                         Height="12"
                         Margin="0,0,8,0"
                         VerticalAlignment="Center">
                    <Ellipse.Fill>
                        <SolidColorBrush x:Name="PostIndicatorBrush"
                                         Color="#F44336" />
                    </Ellipse.Fill>
                </Ellipse>
                <TextBlock Grid.Column="3"
                           Text="Post"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="#555"
                           VerticalAlignment="Center" />
            </Grid>
        </StackPanel>
        
        <!-- Поле для отображения пароля -->
        <StackPanel Grid.Row="4" Margin="0,0,0,15">
            <TextBlock Text="Сгенерированный пароль:" 
                      FontSize="14" 
                      FontWeight="SemiBold"
                      Foreground="#555"
                      Margin="0,0,0,8"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="PasswordTextBox"
                        Style="{StaticResource ModernTextBoxStyle}"
                        IsReadOnly="True"
                        TextAlignment="Center"
                        FontFamily="Consolas"/>
                <Button Grid.Column="1" 
                       x:Name="CopyButton"
                       Content="📋" 
                       Width="50"
                       Height="50"
                       Margin="10,0,0,0"
                       Background="#667eea"
                       Foreground="White"
                       BorderThickness="0"
                       Cursor="Hand"
                       Click="CopyButton_Click"
                       ToolTip="Копировать пароль">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                   CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" 
                                                VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </StackPanel>
        
        <!-- Кнопка сохранения -->
        <Button Grid.Row="5" 
               x:Name="SaveToManagerButton"
               Content="💾 Сохранить в менеджер паролей" 
               Style="{StaticResource ModernButtonStyle}"
               Click="SaveToManagerButton_Click"
               Margin="0,0,0,10"
               HorizontalAlignment="Center"
               Padding="20,12"/>
        
        <!-- Кнопка формального доказательства -->
        <Button Grid.Row="6" 
               x:Name="VerifyButton"
               Content="🔬 Доказать, что сгенерированный пароль гарантированно пройдёт все проверки" 
               Style="{StaticResource ModernButtonStyle}"
               Click="VerifyButton_Click"
               Margin="0,0,0,15"
               HorizontalAlignment="Center"
               Padding="20,12"
               FontSize="14"
               Visibility="Collapsed"/>
        
        <!-- Индикатор надежности пароля -->
        <Border Grid.Row="7" 
               x:Name="SecurityIndicatorBorder"
               CornerRadius="8"
               Padding="15"
               Margin="0,0,0,20"
               Background="#E8E8E8"
               BorderThickness="2"
               BorderBrush="#E0E0E0">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="SecurityStatusText"
                              Text="Сгенерируйте пароль для оценки надежности"
                              FontSize="15"
                              FontWeight="SemiBold"
                              Foreground="#666"
                              VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1"
                              x:Name="SecurityScoreText"
                              FontSize="20"
                              FontWeight="Bold"
                              VerticalAlignment="Center"
                              Margin="10,0,0,0"/>
                </Grid>
                <!-- Индикатор силы пароля -->
                <Grid Margin="0,10,0,0" Height="8">
                    <Border x:Name="SecurityBarBackground"
                           Background="#E0E0E0"
                           CornerRadius="4"/>
                    <Border x:Name="SecurityBarFill"
                           Background="#E0E0E0"
                           CornerRadius="4"
                           HorizontalAlignment="Left"
                           Width="0"/>
                </Grid>
            </StackPanel>
        </Border>
        
        <!-- Дополнительная информация -->
        <TextBlock Grid.Row="8"
                  x:Name="SecurityDetailsText"
                  Text=""
                  FontSize="13"
                  Foreground="#777"
                  TextWrapping="Wrap"
                  Margin="0,0,0,10"
                  Visibility="Collapsed"/>
    </Grid>
</UserControl>

