<UserControl x:Class="CourseWork4Group.Views.PasswordManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Loaded="UserControl_Loaded">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Заголовок -->
        <TextBlock Grid.Row="0" 
                   Text="🔒 Менеджер паролей" 
                   FontSize="32" 
                   FontWeight="Bold"
                   Foreground="#667eea"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>
        
        <!-- Информация о количестве паролей -->
        <TextBlock Grid.Row="1"
                   x:Name="PasswordsCountText"
                   Text="Сохранено паролей: 0"
                   FontSize="14"
                   Foreground="#777"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>
        
        <!-- Список паролей -->
        <ScrollViewer Grid.Row="2"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Disabled">
            <ItemsControl x:Name="PasswordsItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White"
                               CornerRadius="8"
                               Padding="20"
                               Margin="0,0,0,15"
                               BorderBrush="#E0E0E0"
                               BorderThickness="2">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <!-- Сервис -->
                                <TextBlock Grid.Row="0"
                                          Grid.Column="0"
                                          Text="{Binding Service}"
                                          FontSize="18"
                                          FontWeight="Bold"
                                          Foreground="#667eea"
                                          Margin="0,0,0,10"/>
                                
                                <!-- Кнопка удаления -->
                                <Button Grid.Row="0"
                                       Grid.Column="1"
                                       Content="🗑️"
                                       Width="35"
                                       Height="35"
                                       Background="#F44336"
                                       Foreground="White"
                                       BorderThickness="0"
                                       Cursor="Hand"
                                       Click="DeleteButton_Click"
                                       Tag="{Binding Id}"
                                       ToolTip="Удалить пароль">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                   CornerRadius="6">
                                                <ContentPresenter HorizontalAlignment="Center" 
                                                                VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                                
                                <!-- Логин -->
                                <TextBlock Grid.Row="1"
                                          Grid.Column="0"
                                          Grid.ColumnSpan="2"
                                          FontSize="14"
                                          Foreground="#555"
                                          Margin="0,0,0,8">
                                    <Run Text="Логин: " FontWeight="SemiBold"/>
                                    <Run Text="{Binding Login}"/>
                                </TextBlock>
                                
                                <!-- Пароль -->
                                <Grid Grid.Row="2"
                                     Grid.Column="0"
                                     Grid.ColumnSpan="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0"
                                               Orientation="Horizontal"
                                               VerticalAlignment="Center">
                                        <TextBlock FontSize="14"
                                                  Foreground="#555"
                                                  FontWeight="SemiBold"
                                                  Text="Пароль: "
                                                  Margin="0,0,5,0"/>
                                        <TextBlock x:Name="PasswordMaskTextBlock"
                                                  Text="••••••••"
                                                  FontSize="14"
                                                  Foreground="#555"
                                                  VerticalAlignment="Center"/>
                                        <TextBlock x:Name="PasswordTextBlock"
                                                  Text="{Binding Password}"
                                                  FontSize="14"
                                                  FontFamily="Consolas"
                                                  Foreground="#555"
                                                  VerticalAlignment="Center"
                                                  Visibility="Collapsed"/>
                                    </StackPanel>
                                    <Button Grid.Column="1"
                                           x:Name="TogglePasswordButton"
                                           Content="👁️"
                                           Width="30"
                                           Height="30"
                                           Background="#667eea"
                                           Foreground="White"
                                           BorderThickness="0"
                                           Cursor="Hand"
                                           Click="TogglePasswordVisibility_Click"
                                           Tag="{Binding Id}"
                                           ToolTip="Показать/скрыть пароль">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" 
                                                       CornerRadius="6">
                                                    <ContentPresenter HorizontalAlignment="Center" 
                                                                    VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        
        <!-- Пустое состояние -->
        <Border Grid.Row="2"
               x:Name="EmptyStateBorder"
               Background="White"
               CornerRadius="12"
               Padding="40"
               BorderBrush="#E0E0E0"
               BorderThickness="2"
               Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" 
                       VerticalAlignment="Center">
                <TextBlock Text="📋"
                          FontSize="64"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,20"/>
                <TextBlock Text="Нет сохраненных паролей"
                          FontSize="24"
                          FontWeight="SemiBold"
                          Foreground="#555"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,10"/>
                <TextBlock Text="Сохраните пароль из генератора, чтобы он появился здесь"
                          FontSize="16"
                          Foreground="#999"
                          HorizontalAlignment="Center"
                          TextAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

