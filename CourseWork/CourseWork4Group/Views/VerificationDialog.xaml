<Window x:Class="CourseWork4Group.Views.VerificationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Формальное доказательство пароля"
        Height="750"
        Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="#F5F5F5">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0"
                   Text="🔬 Формальная верификация пароля (Триада Хоара)"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="#667eea"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,25"
                   TextWrapping="Wrap"
                   TextAlignment="Center" />

        <!-- Содержимое -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <!-- Постусловие -->
                <Border Background="White"
                        CornerRadius="8"
                        Padding="20"
                        Margin="0,0,0,15"
                        BorderBrush="#E0E0E0"
                        BorderThickness="2">
                    <StackPanel>
                        <TextBlock Text="Постусловие (Postcondition):"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Foreground="#667eea"
                                   Margin="0,0,0,10" />
                        <TextBlock x:Name="PostconditionText"
                                   FontSize="14"
                                   FontFamily="Consolas"
                                   Foreground="#333"
                                   TextWrapping="Wrap"
                                   LineHeight="24" />
                    </StackPanel>
                </Border>

                <!-- Код фрагмента -->
                <Border Background="White"
                        CornerRadius="8"
                        Padding="20"
                        Margin="0,0,0,15"
                        BorderBrush="#E0E0E0"
                        BorderThickness="2">
                    <StackPanel>
                        <TextBlock Text="Код фрагмента генерации:"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Foreground="#667eea"
                                   Margin="0,0,0,10" />
                        <TextBox x:Name="CodeFragmentText"
                                 FontSize="13"
                                 FontFamily="Consolas"
                                 Foreground="#333"
                                 Background="#F9F9F9"
                                 BorderBrush="#E0E0E0"
                                 BorderThickness="1"
                                 Padding="10"
                                 TextWrapping="Wrap"
                                 IsReadOnly="True"
                                 AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto"
                                 Height="150" />
                    </StackPanel>
                </Border>

                <!-- Предусловие и доказательство -->
                <Border Background="White"
                        CornerRadius="8"
                        Padding="20"
                        Margin="0,0,0,15"
                        BorderBrush="#E0E0E0"
                        BorderThickness="2">
                    <StackPanel>
                        <TextBlock Text="Предусловие (Precondition) - WP-движок:"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Foreground="#667eea"
                                   Margin="0,0,0,10" />
                        <TextBlock x:Name="PreconditionText"
                                   FontSize="14"
                                   FontFamily="Consolas"
                                   Foreground="#333"
                                   TextWrapping="Wrap"
                                   LineHeight="24"
                                   Margin="0,0,0,15" />

                        <Separator Margin="0,10,0,10" />

                        <TextBlock Text="Результат доказательства:"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Foreground="#667eea"
                                   Margin="0,10,0,10" />
                        <Border x:Name="ProofResultBorder"
                                Background="#E8F5E9"
                                BorderBrush="#4CAF50"
                                BorderThickness="2"
                                CornerRadius="8"
                                Padding="15">
                            <StackPanel>
                                <TextBlock x:Name="ProofResultText"
                                           FontSize="15"
                                           FontWeight="SemiBold"
                                           Foreground="#2E7D32"
                                           TextWrapping="Wrap"
                                           LineHeight="24" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Триада Хоара -->
                <Border Background="#FFF3E0"
                        CornerRadius="8"
                        Padding="20"
                        Margin="0,0,0,15"
                        BorderBrush="#FF9800"
                        BorderThickness="2">
                    <StackPanel>
                        <TextBlock Text="📐 Триада Хоара:"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Foreground="#E65100"
                                   Margin="0,0,0,10" />
                        <TextBlock x:Name="HoareTripleText"
                                   FontSize="14"
                                   FontFamily="Consolas"
                                   Foreground="#333"
                                   TextWrapping="Wrap"
                                   LineHeight="24" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Кнопка закрытия -->
        <Button Grid.Row="2"
                Content="Закрыть"
                Width="150"
                Style="{StaticResource ModernButtonStyle}"
                HorizontalAlignment="Center"
                Margin="0,10,0,0"
                Click="CloseButton_Click" />
    </Grid>
</Window>

